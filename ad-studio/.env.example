# Ad Studio needs no environment variables to run - it only generates
# creative images locally. It never talks to Facebook itself; publishing
# happens by handing the generated images to Claude (connected to Meta's
# official Ads MCP). See GUIDE.md.
#
# This file is kept as a placeholder in case you later add optional
# extras (e.g. a different port, or cloud storage for uploads).
